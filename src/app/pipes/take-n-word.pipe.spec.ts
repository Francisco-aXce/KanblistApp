import { TakeNWordPipe } from './take-n-word.pipe';

describe('TakeNWordPipe', () => {
  it('create an instance', () => {
    const pipe = new TakeNWordPipe();
    expect(pipe).toBeTruthy();
  });
});
